## 0.0.1

* Add `indicatorFixedWidth` and `indicatorFixedWidthAlignment` so the selected
  tab indicator can use a fixed width without overflowing narrow tabs.
* Apply fixed-width indicator sizing to both the default underline and custom
  indicator decorations.
