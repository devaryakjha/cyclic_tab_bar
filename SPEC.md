# cyclic_tab_bar Spec

## Goal

Initialize this repository as a Flutter package, not a plugin, with a runnable example app that demonstrates package integration.

## Scope

- Create a Flutter package scaffold in the repository root.
- Include the standard `example/` Flutter app generated for packages.
- Keep the initial package implementation minimal and ready for iteration.

## Acceptance Criteria

- `pubspec.yaml` declares a Flutter package named `cyclic_tab_bar`.
- No plugin scaffolding or platform plugin registration files are added.
- `example/` contains a runnable Flutter application that depends on the local package.
- The default tests pass with the available Flutter SDK.
