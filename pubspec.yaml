name: cyclic_tab_bar
description: A Flutter package for cyclic tab bar component with infinite scrolling and full accessibility support.
version: 1.0.0
repository: https://github.com/devaryakjha/cyclic_tab_bar

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.29.2"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
